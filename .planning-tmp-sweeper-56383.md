<!-- hermes-sweeper:review=56383 -->
<!-- hermes-sweeper:review-verdict=keep_open salvageability=medium -->
Thanks for the focused remote-backend investigation. The reported host-mtime issue still exists on current main, but the submitted gate does not cover every current path.

### Problems
- `read_file_tool` still unconditionally calls `file_state.record_read()` at `tools/file_tools.py:1472`. That registry obtains a host mtime at `tools/file_state.py:103-107`; `write_file_tool` and `patch_tool` then call `file_state.check_stale()` first at `tools/file_tools.py:1718` and `:1847`, which stats the host path again at `tools/file_state.py:166-170`. Thus remote file-state warnings retain the same host-path assumption after this patch.

### Suggested changes
- Extend the host-local policy to the FileStateRegistry integration while preserving its per-path locking and valid sibling-write coordination.
- Add remote public-tool coverage for `read_file → write_file` and `read_file → patch_tool`, not only the legacy helper gates. Current main's `_terminal_env_type_for_task()` at `tools/file_tools.py:170-203` already resolves effective container task IDs and is a suitable shared classifier.

This is an automated hermes-sweeper review.
